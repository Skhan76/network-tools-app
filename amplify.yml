version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Skipping build - using pre-built files"
  artifacts:
    baseDirectory: dist/public
    files:
      - '**/*'
  cache:
    paths: []
